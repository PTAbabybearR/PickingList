# 复制为 .env 并填写。.env 已被 git 忽略，勿提交真实密钥。

# 数据库（MVP：本地 SQLite）
DATABASE_URL="file:./dev.db"

# Claude API key（识别用）
ANTHROPIC_API_KEY=""

# 本地 PDF 存储根目录（相对项目根）
STORAGE_DIR="./storage"

# 管理员登录（MVP 简化：单管理员凭据）
ADMIN_PASSWORD="changeme"
